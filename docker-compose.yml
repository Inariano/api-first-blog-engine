services:
  app:
    build: .
    container_name: blog-engine
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    command: npx nodemon src/server.js
    restart: unless-stopped
